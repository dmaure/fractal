{
  "name": "fractal-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Fractal - Generador de aplicaciones multi-framework",
  "scripts": {
    "test": "pnpm -r test",
    "lint": "pnpm -r lint && ./scripts/lint-coupling.sh",
    "lint:coupling": "./scripts/lint-coupling.sh",
    "build": "pnpm -r build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
